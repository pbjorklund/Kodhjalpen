<div class="nav-collapse">
  <ul class="nav">
    <%= get_navigation.inject('') { |result, el| result + el }.html_safe %>
  </ul>
  <ul class="nav pull-right">
    <% if developer_signed_in? %>
      <li class="dropdown">
      <a href="#" class="dropdown-toggle" data-toggle="dropdown">
        <%= current_developer.full_name %> 
        <b class="caret"></b></a>
      <ul class="dropdown-menu">
        <%= link_to "Uppdatera profil", edit_developer_registration_path %>
        <%= link_to "Logga ut", destroy_developer_session_path, :method => :delete %>
      </ul>
      </li>
    <% else %>
      <%= content_tag :li, link_to("Logga in", new_developer_session_path) %>
      <%= content_tag :li, link_to("Registrera dig", new_developer_registration_path) %>
    <% end %>
  </ul>
</div><!--/.nav-collapse -->
